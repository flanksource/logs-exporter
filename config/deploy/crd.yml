apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: elasticmetrics.logs.metrics.flanksource.com
spec:
  group: logs.metrics.flanksource.com
  names:
    kind: ElasticMetric
    listKind: ElasticMetricList
    plural: elasticmetrics
    singular: elasticmetric
  scope: Cluster
  validation:
    openAPIV3Schema:
      description: ElasticMetric is the Schema for the elasticmetrics API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ElasticMetricSpec defines the desired state of ElasticMetric
          properties:
            index:
              type: string
            tuple:
              description: IndexPrefix string  `json:"indexPrefix,omitempty"`
              items:
                properties:
                  aggregate:
                    properties:
                      field:
                        type: string
                      name:
                        type: string
                    type: object
                  filters:
                    additionalProperties:
                      type: string
                    type: object
                  metricName:
                    type: string
                type: object
              type: array
          type: object
        status:
          description: ElasticMetricStatus defines the observed state of Template
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
